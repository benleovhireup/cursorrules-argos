# Cursor rules for [argos](https://github.com/hireupau/argos) repo

Clone to the `.cursor/` directory.

```bash
# in argos project directory:
git clone git@github.com:benleovhireup/cursorrules-argos.git .cursor
```


