---
alwaysApply: true
---

- Plan and prompt me before making any changes.
- App is within `./web-apps/`, start with `npm run start`. Tests use jest, use `fnm use` to switch to the correct node version.
- Add all new labels to the en.json file
- Order attributes alphabetically 

Linting:

- Trailing spaces not allowed
- '||' should be placed at the beginning of the line  
- Missing trailing comma
- No unused variables